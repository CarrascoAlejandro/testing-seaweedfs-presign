meta {
  name: 6 Unauthorized — Direct Filer Write Without Token
  type: http
  seq: 7
}

post {
  url: http://localhost:8888/buckets/inbox/unauthorized-probe.txt
  body: multipartForm
  auth: none
}

body:multipart-form {
  file: probe
}

tests {
  test("SeaweedFS rejects write with no JWT — 401", function() {
    expect(res.status).to.equal(401);
  });
}
